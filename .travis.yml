language: node_js
script:
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  local_dir: _site
  github-token: $GITHUB_TOKEN
  keep-history: true
  target_branch: gh-pages
  on:
    branch: master
